[Unit]
Description=Sonarr - TV automation
After=network-online.target
Wants=network-online.target
RequiresMountsFor=/home/skim/jellyfin-configs/sonarr /mnt/media/downloads /mnt/media/tv

[Service]
ExecStartPre=/usr/bin/podman rm -f sonarr
ExecStart=/usr/bin/podman run --name sonarr --rm \
  --network host \
  -e PUID=1000 \
  -e PGID=1000 \
  -e TZ=America/New_York \
  -v /home/skim/jellyfin-configs/sonarr:/config \
  -v /mnt/media/tv:/tv:rw \
  -v /mnt/media/downloads:/downloads:rw \
  docker.io/linuxserver/sonarr:latest
Restart=on-failure
RestartSec=10
ExecStop=/usr/bin/podman stop -t 30 sonarr

[Install]
WantedBy=multi-user.target